<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usermanagement.mapper.PasswordResetMapper">

    <resultMap id="PasswordResetMap" type="com.example.usermanagement.entity.PasswordReset">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="resetToken" column="reset_token"/>
        <result property="expireTime" column="expire_time"/>
        <result property="used" column="used"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="PasswordResetMap">
        SELECT id, user_id, reset_token, expire_time, used, create_time
        FROM sys_password_reset
        WHERE id = #{id}
    </select>

    <select id="selectByResetToken" resultMap="PasswordResetMap">
        SELECT id, user_id, reset_token, expire_time, used, create_time
        FROM sys_password_reset
        WHERE reset_token = #{resetToken}
    </select>

    <insert id="insert" parameterType="com.example.usermanagement.entity.PasswordReset" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_password_reset (user_id, reset_token, expire_time, used, create_time)
        VALUES (#{userId}, #{resetToken}, #{expireTime}, #{used, jdbcType=INTEGER}, NOW())
    </insert>

    <update id="update" parameterType="com.example.usermanagement.entity.PasswordReset">
        UPDATE sys_password_reset
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="resetToken != null">reset_token = #{resetToken},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
            <if test="used != null">used = #{used},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="markAsUsed">
        UPDATE sys_password_reset SET used = 1 WHERE id = #{id}
    </update>

    <delete id="deleteByUserId">
        DELETE FROM sys_password_reset WHERE user_id = #{userId}
    </delete>
</mapper>
