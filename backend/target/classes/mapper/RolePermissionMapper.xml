<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.usermanagement.mapper.RolePermissionMapper">

    <resultMap id="RolePermissionMap" type="com.example.usermanagement.entity.RolePermission">
        <id property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="permissionId" column="permission_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectByRoleId" resultMap="RolePermissionMap">
        SELECT id, role_id, permission_id, create_time
        FROM sys_role_permission
        WHERE role_id = #{roleId}
    </select>

    <select id="selectByPermissionId" resultMap="RolePermissionMap">
        SELECT id, role_id, permission_id, create_time
        FROM sys_role_permission
        WHERE permission_id = #{permissionId}
    </select>

    <insert id="insert" parameterType="com.example.usermanagement.entity.RolePermission" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role_permission (role_id, permission_id, create_time)
        VALUES (#{roleId}, #{permissionId}, NOW())
    </insert>

    <insert id="batchInsert">
        INSERT INTO sys_role_permission (role_id, permission_id, create_time)
        VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId}, NOW())
        </foreach>
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM sys_role_permission WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteByPermissionId">
        DELETE FROM sys_role_permission WHERE permission_id = #{permissionId}
    </delete>

    <delete id="deleteByRoleIdAndPermissionIds">
        DELETE FROM sys_role_permission
        WHERE role_id = #{roleId} AND permission_id IN
        <foreach collection="permissionIds" item="permissionId" open="(" separator="," close=")">
            #{permissionId}
        </foreach>
    </delete>
</mapper>
